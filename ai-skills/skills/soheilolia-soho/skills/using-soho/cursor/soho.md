---
description: "Activate Soho mode: methodology first, orchestration when justified, receipts always"
---

Use the `soho:using-soho` skill first. Then determine whether the task should run in `solo`, `swarm`, or `recommend` mode. Emit an explicit Soho receipt at the end unless the user opted out.
