---
description: "Use Soho swarm mode with explicit roles, topology, and synthesis"
---

Use `soho:using-soho`, then `soho:orchestrating-swarms`, `soho:selecting-topology`, and `soho:synthesizing-results`. If the host cannot actually spawn agents, say so and run the role passes serially with `runtime: prompt-backed`.
